"""Publish demo clip WAV into a LiveKit room as an audio track. STUB."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

print("play_clip.py stub — real impl in Phase 1 uses livekit.rtc to publish WAV frames.")
